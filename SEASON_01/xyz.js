 
// rrequire("./path")
// All the code of the module wrapped inside a function(IIFE)

// Immediately Invoked Function Expressiona => IIFE

(function () {
    // All code of the module runs inside the function
})();  

// To check path of the file
console.log(__filename);

// To check code or repo directory
console.log(__dirname);